# ThreatFox API auth key from https://auth.abuse.ch/
THREATFOX_AUTH_KEY=your-auth-key-here

# Optional: number of days for recent IOCs (1–7, default 1)
# THREATFOX_DAYS=1

# AnythingLLM API key from https://anythingllm.com/
ANYTHINGLLM_API_KEY=your-api-key-here

# Optional: AnythingLLM base URL (default http://localhost:3001). API docs at {base}/api/docs/
# ANYTHINGLLM_BASE_URL=http://localhost:3001

# Optional: workspace by name (e.g. "OpenAI Chats"). Used to pick which workspace to chat in;
# also used as the name when creating a workspace if none exist.
# ANYTHINGLLM_WORKSPACE_NAME=OpenAI Chats

# ThreatFox daily report tool: workspace name for ThreatFox IOCs/threads (default "ThreatFox Daily")
# ANYTHINGLLM_THREATFOX_WORKSPACE=ThreatFox Daily

# ThreatFox daily report: LLM model to use for chat (e.g. OpenAI gpt-5.2). Passed in chat request body if supported by your AnythingLLM instance.
# ANYTHINGLLM_LLM_MODEL=

# ThreatFox daily report: set to 1/true/yes to generate analyst questions via AnythingLLM's /v1/openai/chat/completions (uses workspace docs + AnythingLLM auth only)
# USE_LLM_QUESTIONS=
